import com.soywiz.korim.color.*

class Gesteinsplanet(umlaufbahn: Umlaufbahn,
                farbe : RGBA,
                radius : Int) : Planet(umlaufbahn = umlaufbahn, farbe = farbe, radius = radius)